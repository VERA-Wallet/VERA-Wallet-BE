/**
 * 정규화 규칙 버전.
 *
 * 분류는 "원본 체인 데이터 × 규칙"의 함수인데, 저장된 행은 색인 당시 규칙의 결과를 그대로 들고 있다. 동기화는
 * 마지막 블록 이후만 가져오므로(BindingChainCursor), 규칙을 고쳐도 이미 지나간 블록의 행은 다시 판정되지 않는다 —
 * 2026-09-11에 위조 아웃바운드(홈오글리프 "EТH", 티커가 딱 "ETH"인 ERC-20) 규칙이 들어갔지만 9월 8일에 색인된
 * 지갑에는 그 규칙 이전의 "송금" 판정이 남아 있던 것이 그 예다.
 *
 * 그래서 커서는 마지막 블록과 함께 "어느 규칙 버전으로 걸었는가"를 기록하고, 이 상수보다 낮은 버전의 체인은 다음
 * 동기화 때 처음부터 다시 걷는다. 다시 걷기는 평소의 편집 보존 업서트와 대체 행 정리를 그대로 타므로 사용자 편집
 * (user_override)은 살아남고, 이벤트 종류가 바뀐 행(스왑 → 스팸 등)은 유령으로 남지 않고 정리된다.
 *
 * 올려야 할 때: 스팸 판정(spam-filter.ts), 다리 넷팅·스왑 판정·브릿지 추정(indexer.adapters.ts) 등 "같은 원본에서
 * 다른 행"을 만들게 하는 모든 변경. 가격·표시만 바뀌면 올리지 않는다(가격은 별도 보강 경로가 채운다).
 *
 * 1: 위조 아웃바운드 Transfer 로그를 스팸으로 거르는 규칙(a98eba2)까지 반영. 마이그레이션 전 커서는 0으로 저장돼
 *    있어 배포 후 첫 동기화에서 모든 체인을 한 번 다시 걷는다.
 */
export const NORMALIZATION_RULES_VERSION = 1;

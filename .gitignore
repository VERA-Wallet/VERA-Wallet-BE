node_modules
dist
coverage
.env
*.log
*.tsbuildinfo
.DS_Store
apps/backend/node_modules
packages/*/node_modules
apps/backend/prisma/dev.db

.gjc/
# OMC runtime state (any depth). Project-scoped skills under .omc/skills are the committable exception.
.omc/
!.omc/skills/

# .env 백업본(자격증명 포함). 편집 전에 복사해 두는 관례라 저장소에 절대 올리지 않는다.
**/.env.bak-*
